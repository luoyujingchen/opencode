# Release build environment template
# 1) Copy this file to .env when building locally, or load these vars in your shell.
# 2) Set OPENCODE_VERSION to the upstream opencode tag without the leading "v".

OPENCODE_CHANNEL=latest
OPENCODE_RELEASE=1
OPENCODE_VERSION=1.3.3
